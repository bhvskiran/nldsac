class Solution {
  /**
   * @param {number} D
   * @param {number} Q
   * @return {number}
   */
  floorDivision(D, Q) {
    // Your code goes here
    const fd = Math.floor(D / Q);
    return fd;
  }
}
